# AI エージェント運用ガイド

- 状態: 採用済み（Accepted）
- 管理責任者: リポジトリメンテナー
- 最終更新日: 2026-08-15

## 目的

この文書は、Hakutaku で作業する AI エージェントが自動検出する入口であり、作業の流れ、並行作業、サブエージェント、検証、引き継ぎに関するリポジトリ固有の唯一の正本です。プロダクト仕様、Git / PR の規則、セキュリティポリシーは複製せず、それぞれの正本を参照します。

エージェント規則はこのファイルへ集約します。入れ子の `AGENTS.md` が必要な場合は、そのディレクトリだけに適用する差分を記載し、この文書や共通規則を複製しません。

Codex、Claude Code など、どの AI エージェントを使う場合もこの文書が正本です。ツールが別名のファイルを自動検出する場合（例: Claude Code のルート [`CLAUDE.md`](CLAUDE.md)）は、そのファイルをこの文書への入口に限定し、規則本文を複製しません。

人間と AI には同じ品質・セキュリティ基準を適用します。AI が生成した差分を採用する責任は、その作業を依頼、レビュー、マージする人間にあります。

Issue、プルリクエスト、コメント、レビュー、コミットメッセージ、引き継ぎ、文書、利用者への報告は、[使用言語と文章の規則](docs/development/repository-operations.md#使用言語と文章)に従い、原則として分かりやすい日本語で書きます。

## 指示と正本の扱い

1. システム、開発者、利用者の明示的な指示と、適用される `AGENTS.md` を確認する
2. この `AGENTS.md` を全文読む
3. [`docs/README.md`](docs/README.md) でタスクに対応する正本を選ぶ
4. 個別タスクの目的、受け入れ条件、動的状態は GitHub Issue、差分と検証結果は下書き PR / PR を正本とする。GitHub を更新できないローカル作業では、利用者が明示した現在のタスクを暫定記録とする
5. 現在の worktree、ソース、テスト、Git の差分を実態の証拠として確認する

チャット履歴、エージェントの記憶、引き継ぎ文だけを現在状態の証拠にしません。文書と実装が矛盾する場合は推測で統合せず、どちらが正本かを確認して同じ変更内で整合させます。`Draft`、`Proposed`、`TBD` は、採用済み（`Accepted`）の仕様として扱いません。

## 必読文書

### 毎回のセッション

1. この `AGENTS.md` 全文
2. [`README.md`](README.md) のプロジェクト状態
3. [`docs/README.md`](docs/README.md) の文書一覧
4. 変更作業では[`docs/development/README.md`](docs/development/README.md)
5. タスクの Issue / PR、受け入れ条件、既存の担当範囲の宣言。ローカル作業の例外では、利用者が明示した現在のタスク

### タスク別の読み分け

| タスク | 必読の正本 |
| --- | --- |
| プロダクトの目的・範囲・利用者挙動 | `docs/product/vision.md` → `docs/product/scope.md` → 要件 |
| 用語、項目（Item）、時刻、相関 | `docs/domain/glossary.md` → `docs/domain/time-model.md` → 要件 |
| コンポーネント、コネクター、データモデル | `docs/architecture/overview.md` → `connector-contract.md` → 採用済み ADR |
| 認証情報、キャッシュ、エクスポート、ネットワーク、ファイル解析 | `docs/security/data-handling.md` を必ず追加 |
| 技術選定・横断変更 | 要件 → アーキテクチャ → 採用済み ADR → ロードマップ |
| コード実装・コメントの記述 | `docs/development/code-comments.md` |
| Git / PR / レビュー / マージ | `docs/development/workflow.md` |
| 複数セッション、worktree、引き継ぎ | `docs/development/concurrent-sessions.md` |
| エージェント規則、Codex や Claude Code のリポジトリ設定 | この `AGENTS.md` → `docs/development/repository-operations.md` |
| 単純な誤字・リンク修正 | 対象文書と、その正本関係だけ |

## セッションの流れ

### 開始

1. 依頼の目的、成果物、変更可否、禁止事項、受け入れ条件を特定する
2. 必読文書を読み、`Draft`、`TBD`、未決事項を確認する
3. `git status --short --branch`、`git diff`、`git worktree list` 相当でブランチ、worktree、既存変更を確認する
4. 並行作業ならセッション専用の worktree とブランチであること、Issue に記録した担当範囲と予定するファイル範囲が重複しないことを確認する
5. 変更対象、対象外、検証方法、文書・ADR の更新要否を決める
6. 独立して進められる小タスクだけを並列化候補とし、書き込み担当をファイル単位で一意にする
7. 重要な仮定、権限不足、外部への影響、停止要因を作業前に共有する

GitHub の動的状態を確認・更新できない、または GitHub の書き込み権限がない場合は、その制約を報告します。利用者から明示的に割り当てられたローカル範囲と現在のタスクを暫定的な担当記録として作業できますが、リポジトリ全体の担当権限を取得したとは扱いません。同一利用者が管理する現在のタスクの一対一の継続・引き継ぎだけは、Issue なしで許可します。それ以外の独立タスクとの共有、リモート送信、下書き PR 作成、リポジトリ単位の引き継ぎを行う前に、利用者または権限を持つ人が着手可能（`Ready`）な Issue へ同期します。未割り当ての横断変更や、既存の担当範囲と競合し得る変更は開始しません。

### 作業中

- 一度に一つの目的を扱い、対象範囲外の変更と無関係な整形を混ぜない
- 意味のある変更単位ごとに差分と対象検証を確認する
- 別セッションの差分や意図不明の変更を見つけたら上書きせず、重複範囲を特定して停止・調整する
- `TBD` を実装都合で決めない。必要なら選択肢、根拠、トレードオフを提示する
- 利用者向けの挙動、形式、互換性、信頼境界が変わる場合は正本文書を同時に更新する
- 長時間作業では、完了済み・進行中・次の確認を簡潔に共有する
- 人が読む文章は分かりやすい日本語で書き、原語を保つ必要がある箇所には日本語の説明を添える
- サブエージェントの対象範囲、ファイルの担当、結果、競合を主エージェントが監督する

### 中断または一時停止

可能なら、状態が一貫した安全な地点で停止します。途中の移行、生成途中のファイル、実行中のプロセスを説明なしで放置しません。

1. 完了、変更中、未着手を区別する
2. ブランチ、worktree、最新コミット、未コミット差分、変更ファイルを記録する
3. 実行済みの検証と結果、未実施項目、既知のリスクを記録する
4. 停止要因、解除条件、必要な人間判断を記録する
5. サブエージェントの状態と、採用済み・未検証の成果を記録する
6. 再開時の最初の具体的な操作を一つ示す

記録済みのリモートブランチとコミットから後任が取得できない状態を引き継ぐ場合は、後任が実際に復元できる、秘密情報を含まないタスク、スナップショット（snapshot）、worktree の識別名と、「現在復元可能（`Recoverable now`）」かどうかを引き継ぎに記録します。対象には未コミット差分、未送信のコミット、スナップショットだけにある変更を含みます。復元先を示せない状態は、引き継ぎ未成立です。

コミットやリモート送信がタスクで許可されている場合、別セッションへ引き継ぐ差分は作業途中のコミットとしてリモートブランチに保存します。許可されていない場合は勝手にコミットやリモート送信をせず、差分がローカルにしかないことを明示して、利用者へ保存方法を確認します。

### 再開または引き継ぎ

1. この `AGENTS.md` を再読し、規則の変更を確認する
2. 引き継ぎ記録だけを信じず、Git の状態、差分、コミット、Issue / PR で現状を再検証する
3. 目的、受け入れ条件、現在状態、制約、次の操作を復元する
4. 担当の移管が Issue / PR で受諾済みか確認する。Issue / PR のないローカルタスクでは、利用者による明示的な再割り当てと現在のタスクの引き継ぎを確認する
5. サブエージェントや前セッションの成果をレビュー・検証してから採用する
6. 所有者や意図が不明な差分は変更せず、確認する

引き継ぎには[テンプレート](docs/development/templates/handoff.md)を使います。セッションが停止しただけでは担当は移りません。原則として、Issue / PR 上で後任が受諾を記録した時点で成立します。Issue / PR のないローカルタスクでは、同一利用者が後任へ明示的に再割り当てし、引き継ぎ内容と復元場所を現在の Codex タスクまたは利用者管理のタスク記録へ渡した時点で、一対一の継続として成立します。この代替手順は、独立タスクへ共有する状態の正本には使いません。

### 完了

1. すべてのサブエージェントを回収し、主エージェントが成果を統合・再検証する
2. 受け入れ条件を一項目ずつ、ソース、テスト、表示結果、差分などの直接的な根拠で監査する
3. `git status` と差分全体で、対象範囲外の変更、デバッグ用の生成物、認証情報、実データの混入を確認する
4. リスクに応じたテスト、静的解析、整形、文書リンク、手動確認を実行する
5. 要件、文書、ADR、Issue / PR の更新要否を確認する
6. 実行できなかった検証と残るリスクを隠さず報告する
7. 期待する結果、変更したファイル、検証結果、残る項目の順で、分かりやすい日本語を使って最終報告する

「エラーを見つけなかった」ことを完了の証拠にせず、各受け入れ条件を検証できた場合だけ完了とします。

## worktree と変更の安全性

Worktree、ブランチ、担当範囲、競合、後片付けの規則は[`concurrent-sessions.md`](docs/development/concurrent-sessions.md)を正本とし、作業前に対象の節を読みます。

AI 固有の追加規則:

- 別の独立セッションのブランチ、worktree、未コミット変更、stash、プロセスを操作しない
- 主セッション内のサブエージェントは独立セッションとは数えず、主セッションが割り当てた重複しないファイル範囲に限って、共有 worktree へ書き込める。これが同時書き込みの唯一の例外
- Git 操作の前後に状態と差分を確認し、自分の対象範囲以外をステージ、復元、後片付けしない

## サブエージェントの手順

### 委譲する場合

- 相互依存しない調査、複数領域の独立レビュー、重複しないファイルの実装・テスト
- 並行化による時間短縮が、調整と統合のコストを上回る

### 委譲しない場合

- 同じファイルまたは近接行を複数エージェントが編集する作業
- 一つの判断へ逐次依存する小タスク
- プロダクト・アーキテクチャの最終判断、受け入れ判定、最終報告
- 認証情報や実データを必要とするタスク

### 委譲時に伝える内容

主エージェントはサブエージェントへ次を明示します。

- 目的と期待する成果物
- 正確なファイル・ディレクトリの範囲と、読み取り専用か書き込み可能か
- 対象外と禁止事項
- 読む正本と検証方法
- 共有 worktree か分離した worktree か
- 既存変更を保持し、対象範囲外を編集せず、コミットやリモート送信をしないこと

共有 worktree では書き込み担当をファイルごとに一意にし、重複を検出したサブエージェントは編集せず、主エージェントへ返します。主エージェントだけが横断統合、差分全体のレビュー、最終検証、完了判断を行います。

サブエージェントの結果は「要約 / 変更ファイル / 検証 / リスク / 後続課題」で受け取り、主エージェントが内容と現在状態を検証します。

## 停止して確認を求める条件

次の場合は、安全にできる調査まで行ったうえで変更を停止し、具体的な判断または権限を求めます。

- 依頼が読み取り専用の原則またはセキュリティポリシーと衝突する
- 既存変更の所有者・意図が不明で、安全に分離できない
- 未決のプロダクト・アーキテクチャ判断によって結果が大きく変わる
- 認証情報、本番データ、個人情報が必要、または検出された
- 破壊的操作、外部への影響、追加権限、リポジトリ設定の変更が必要
- 高リスク変更を検証できず、安全性を評価できない
- 他セッションと同じブランチ、worktree、ファイル範囲への書き込みが避けられない

阻害要因を報告するときは、阻害条件、確認済みの根拠、試した代替案、解除に必要な最小の判断を記載します。
